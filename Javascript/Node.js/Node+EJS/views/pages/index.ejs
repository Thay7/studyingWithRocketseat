<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head'); %>

    <body class="container">

        <%- include('../partials/header' , {pagina: 'Home'}); %>

            <main>
                <div class="jumbotron">
                    <h1>Node com EJS</h1>
                    <p>Vamos aprender a trabalhar com arquivos EJS</p>
                    <p><%= subtitle %></p>
                </div>
                <ul>
                    <% qualitys.forEach(function(quality){ %>  <%# nao colocar o traço da sintaxe por que irei usar codigo js%>
                        <li>
                            <strong><%=quality.title %></strong><%= quality.message %> <%# Usando o = por que quero printar na tela %>
                        </li>
                    <% })  %>
                </ul>

            </main>

            <%- include('../partials/footer'); %>

    </body>

</html>